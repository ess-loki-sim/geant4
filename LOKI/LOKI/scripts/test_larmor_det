#!/bin/bash

ARGS="det_only=true"
ess_loki_larmor_sim $ARGS -lESS_Empty |grep '^G4Launcher::Launcher'
if [ ${PIPESTATUS[0]} == 0 ]; then
    ess_loki_larmor_det_mcpl larmor_CountingGas_Converter.griff
else
    exit 1
fi
